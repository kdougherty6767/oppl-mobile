# Changelog

## 1.1.0+2
- Added full lineup-and-scoring flow with breaker indicators and round/game score entry (with BR flag).
- Added membership expiry tracking (date picker, status, filter/sort) in admin user list.
- Added team selection and match list; lineup auto-fill and scorecard preview on mobile.
- Enforced email verification gate on mobile; registration mirrors web flow.
- Set Android package name to `com.kgdappdevelopment.oppl` and app name to OPPL Mobile; prepared signing config via `key.properties`.
- Added data deletion page (`/data-deletion`) for compliance.
